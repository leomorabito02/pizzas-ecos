services:
  - type: web
    name: pizzas-ecos-backend
    env: docker
    dockerfilePath: ./backend/Dockerfile
    region: oregon
    plan: free
    
    # Variables de entorno
    envVars:
      - key: PORT
        value: "8080"
      - key: CORS_ALLOWED_ORIGINS
        value: "http://localhost:5000,https://ecos-ventas-pizzas.netlify.app"
      - key: credsJSON
        value: venta-pizzas-ecos.json
      - key: SpreadsheetID
        value: 1E8bLD1DKp3ZrsmLb05O7cAJ-Qn929yBSTrZ18BSeVk0
    
    # Variables de entorno secretas (agregar en Render Dashboard)
    # DATABASE_URL: mysql://pizzas_app:password@host:port/ecos
    # JWT_SECRET: tu-secret-key
    
    # Archivos estáticos (credenciales)
    # Nota: Agregar venta-pizzas-ecos.json en Render como secret file
