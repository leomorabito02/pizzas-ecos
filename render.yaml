services:
  - type: web
    name: pizzas-ecos-backend
    env: go
    envVars:
      - key: GOVERSION
        value: "1.25.5"
    buildCommand: "cd backend && go build -o pizzas-ecos"
    startCommand: "./pizzas-ecos"
    plan: free
    region: oregon
    
    # Variables de entorno
    envVars:
      - key: PORT
        value: "8080"
      - key: credsJSON
        value: venta-pizzas-ecos.json
      - key: SpreadsheetID
        value: 1E8bLD1DKp3ZrsmLb05O7cAJ-Qn929yBSTrZ18BSeVk0
    
    # Archivos estáticos (credenciales)
    # Nota: Agregar venta-pizzas-ecos.json en Render como secret file
